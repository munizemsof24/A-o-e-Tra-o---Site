const https = require('https'); https.get('https://ibb.co/jvSGC8Lb', (res) => { let data = ''; res.on('data', d => data += d); res.on('end', () => { const matches = data.match(/https:\/\/i\.ibb\.co\/[^\"'\\[<]+\.(jpg|jpeg|png|avif|webp)/); console.log(matches ? matches[0] : 'not found'); }) });
